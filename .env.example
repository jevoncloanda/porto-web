# Public origin of the deployed site. Used for canonical URLs and Open Graph
# metadata. No trailing slash. Falls back to http://localhost:3000 when unset.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
